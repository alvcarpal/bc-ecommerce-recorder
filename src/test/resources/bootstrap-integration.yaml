management:
  server:
    port: 8192
  endpoints:
    web:
      base-path: /
server:
  port: 8080
  servlet:
    context-path: /@project.artifactId@
project:
  version: @project.version@
spring:
  application:
    name: album-enrichment
  cloud:
    config:
      enabled: false
